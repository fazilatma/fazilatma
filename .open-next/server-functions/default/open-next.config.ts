import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // تنظیمات پیش‌فرض OpenNext برای Cloudflare Workers کافی است.
  // ISR و کش اضافی در این پروژه استفاده نمی‌شود.
});
